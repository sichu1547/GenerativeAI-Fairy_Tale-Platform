# services: for local
#   web:
#     # build: .
#     command: python manage.py runserver 0.0.0.0:8000
#     volumes:
#       - ./:/usr/src/app/
#     ports:
#       - 8000:8000
#     env_file:
#       - ./.env.dev

services:
  web:
    image: ${{ secrets.DOCKER_HUB_USERNAME }}/dev-web:latest
    ports:
      - "8000:8000"
    command: >
      sh -c "
      python manage.py migrate &&
      python manage.py runserver 0.0.0.0:8000
      "